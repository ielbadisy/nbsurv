# CRAN submission comments — nbsurv 0.3.1

## Test environments

* Local: Ubuntu 24.04, R 4.4.x
* win-builder: R-devel
* rhub: windows-x86_64-devel, ubuntu-gcc-devel

## R CMD check results

0 ERRORs | 0 WARNINGs | 1 NOTE (locally; will drop to 0 once the release
blocker below is resolved)

The NOTE is "New submission", expected for a first CRAN submission, plus
URL 404s for the GitHub repo/issues/CI links because the repository is
currently **private**. Making `github.com/ielbadisy/nbsurv` public before
submission resolves the URL check.

## Reverse dependencies

None (new package).
